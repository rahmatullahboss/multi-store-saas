import type { ReactElement } from 'react';

// MVP fallback preview component to satisfy section registry imports.
export function SocialProofSectionPreview(): ReactElement | null {
  return null;
}

export default SocialProofSectionPreview;
