import type { ReactElement } from 'react';

// MVP fallback: disables builder floating actions on storefront pages.
export function FloatingActionButtons(): ReactElement | null {
  return null;
}

export default FloatingActionButtons;
