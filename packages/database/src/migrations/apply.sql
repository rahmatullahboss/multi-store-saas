-- apply.sql
--
-- Local-only convenience script used by:
--   npm --workspace packages/database run migrate
--
-- Keep this file as a safe no-op by default. If you need to run a one-off local patch,
-- put it here temporarily.
SELECT 1;

