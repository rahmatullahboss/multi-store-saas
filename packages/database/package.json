{
  "name": "@ozzyl/db",
  "version": "0.0.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "license": "MIT",
  "scripts": {
    "generate": "drizzle-kit generate",
    "migrate": "wrangler --config ../../apps/web/wrangler.toml d1 migrations apply multi-store-saas-db --local",
    "migrate:prod": "wrangler --config ../../apps/web/wrangler.toml d1 migrations apply multi-store-saas-db --remote",
    "studio": "drizzle-kit studio",
    "lint": "eslint ."
  },
  "dependencies": {
    "drizzle-orm": "^0.38.0"
  },
  "devDependencies": {
    "drizzle-kit": "^0.30.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.63.0"
  }
}
