---
name: typescript
description: "Typescript for multi-store-saas. 2 fixes."
domain: typescript
triggers:
  - glob: "**/*.ts"
  - glob: "**/*.tsx"
autoGenerated: true
enabled: true
---

# Typescript

Auto-compiled from **9 real patterns** in **multi-store-saas**. This skill is auto-routed to agents when working on typescript files.

## 🔧 Problem Playbooks

### Fixed null crash in FRAUD — prevents null/undefined runtime crashes
-       if (!settings.enabled) {
+       const assessmentResult = await performFraudCheck({
-         return json({
+         phone,
-           riskScore: 0,
+         storeId,
-           decision: 'allow',
+         orderTotal,
-           signals: [],
+         paymentMethod,
-           isBlacklisted: false,
+         shippingAddress,
-           message: 'Fraud detection is disabled',
+     

**Actionable Steps:**
1. Modified 1 files
2. identifier: FRAUD
3. identifier: API
4. identifier: Check
5. identifier: Error

### Fixed null crash in MiddlewareHandler
- const SUSPICIOUS_EXTENSIONS = [
+ function isSuspiciousPath(path: string): boolean {
-   '.php', '.env', '.git', '.sql', '.bak', '.jsp', '.asp', '.aspx', '.yaml', '.yml', '.ini', '.conf', '.json'
+   const lowerPath = path.toLowerCase();
- ];
+   
- 
+   const badExtensions = ['.php', '.env', '.sql', '.bak', '.jsp', '.asp', '.aspx', '.ini', '.conf', '.tar.gz', '.zip'];
- function isSuspiciousPat

**Actionable Steps:**
1. Modified 1 files
2. identifier: MiddlewareHandler
3. identifier: Bindings
4. identifier: TenantEnv
5. identifier: Variables

## 🤔 Decisions & Trade-offs

- **decision in storefront-data.server.ts** — -     slug: p.slug || null,
+     slug: (p as Record<string, unknown>).slug as string | null || null

---
*Auto-generated by BrainSync 🧠 | 9 patterns | 2026-03-26*
