---
name: typescript-react
description: "Typescript React for multi-store-saas. 5 conventions, 9 fixes."
domain: typescript-react
composesFrom:
  - react
triggers:
  - glob: "**/*.tsx"
autoGenerated: true
enabled: true
---

# Typescript React

Auto-compiled from **39 real patterns** in **multi-store-saas**. This skill is auto-routed to agents when working on typescript-react files.

## 🔧 Problem Playbooks

### Fixed null crash in MetaFunction — parallelizes async operations for speed
- import { drizzle } from 'drizzle-orm/d1';
+ import { orders, orderItems, stores } from '@db/schema';
- import { orders, orderItems, stores } from '@db/schema';
+ import { eq, and } from 'drizzle-orm';
- import { eq, and } from 'drizzle-orm';
+ import { useEffect, useRef } from 'react';
- import { useEffect, useRef } from 'react';
+ import { trackingEvents } from '~/utils/tracking';
- import { tr

**Actionable Steps:**
1. Modified 1 files
2. identifier: MetaFunction
3. identifier: Don
4. identifier: LoaderFunctionArgs
5. identifier: Response

### Fixed null crash in Date — prevents null/undefined runtime crashes
-   const formatDate = (date: string | Date) => {
+   const formatDate = (date: string | Date | null) => {
-     return new Date(date).toLocaleDateString(lang === 'bn' ? 'bn-BD' : 'en-US', {
+     if (!date) return '—';
-       year: 'numeric',
+     return new Date(date).toLocaleDateString(lang === 'bn' ? 'bn-BD' : 'en-US', {
-       month: 'long',
+       year: 'numeric',
-       day: 'numeric',

**Actionable Steps:**
1. Modified 1 files
2. identifier: Date
3. identifier: Link
4. identifier: ArrowLeft
5. identifier: StatusBadge

### Fixed null crash in String — avoids unnecessary re-renders in React
-       return json(
+       const raw = resultObj as any;
-         { success: true, intent: 'FRAUD_CHECK', orderId, riskResult: { ...resultObj, fromCache: false } },
+       const successRate = 100 - (raw.riskScore ?? raw.score ?? (raw.isHighRisk ? 100 : 0));
-         { headers: { 'x-order-id': String(orderId) } }
+       const normalizedResultObj = {
-       );
+         successRate,
-     } c

**Actionable Steps:**
1. Modified 1 files
2. identifier: String
3. identifier: Fraud
4. identifier: Error
5. identifier: Default

### Fixed null crash in Failed — prevents null/undefined runtime crashes
-     await saveUnifiedStorefrontSettingsWithCacheInvalidation(
+     try {
-       db,
+       await saveUnifiedStorefrontSettingsWithCacheInvalidation(
-       context.cloudflare.env,
+         db,
-       storeId,
+         context.cloudflare.env,
-       { 
+         storeId,
-         heroBanner: { slides },
+         { 
-         announcement: announcementText ? {
+           heroBanner: { s

**Actionable Steps:**
1. Modified 1 files
2. identifier: Failed
3. identifier: Legacy
4. identifier: Sync
5. identifier: Date

### Patched security issue Product — avoids unnecessary re-renders in React
-         />
+           shippingConfig={data.shippingConfig}
-       ) : (
+         />
-         <div className="text-center py-20">
+       ) : (
-           <h1 className="text-2xl font-bold text-red-600">Product Page Template Not Found</h1>
+         <div className="text-center py-20">
-         </div>
+           <h1 className="text-2xl font-bold text-red-600">Product Page Template Not Found

**Actionable Steps:**
1. Modified 1 files
2. identifier: Product
3. identifier: Page
4. identifier: Template
5. identifier: Not

### problem-fix in CartPage.tsx
File updated (external): apps/web/app/components/store-templates/dc-store/pages/CartPage.tsx

Content summary (245 lines):
/**
 * DC Store Cart Page
 * 
 * Based on the original DC Store design with golden gradient theme.
 * Features clean cart layout with product cards and order summary.
 */

import { Link } from 'react-router';
import { ShoppingCart, Trash2, Plus, Minus, ArrowRight } from 'lucid

**Actionable Steps:**
1. Modified 1 files

### problem-fix in ProductPage.tsx
-                       ⭐ রেটিং {averageRating.toFixed(1)}/৩
+                       ⭐ রেটিং {averageRating.toFixed(1)}/৫

📌 IDE AST Context: Modified symbols likely include [DCProductPageProps, DCProductPage]

**Actionable Steps:**
1. Modified 1 files

### Fixed null crash in Brain — parallelizes async operations for speed
- import { customers, stores, orders, products, orderItems, pageViews, carts, checkoutSessions } from '@db/schema';
+ import {
- import { requireSuperAdmin } from '~/services/auth.server';
+   customers,
- import { callAIWithSystemPrompt } from '~/services/ai.server';
+   stores,
- import { useState, useEffect } from 'react';
+   orders,
- import {
+   products,
-   Brain,
+   orderItems,
-   Tren

**Actionable Steps:**
1. Modified 1 files
2. identifier: Brain
3. identifier: TrendingUp
4. identifier: TrendingDown
5. identifier: AlertTriangle

### Fixed null crash in Handle — prevents null/undefined runtime crashes
- export async function loader({ request, context }: LoaderFunctionArgs) {
+ /**
-   // Resolve store
+  * Handle POST/PUT/DELETE to unknown routes (bots probing /graphql, /wp-admin, etc.)
-   const storeContext = await resolveStore(context, request);
+  */
- 
+ export async function action({ request }: ActionFunctionArgs) {
-   if (!storeContext) {
+   const url = new URL(request.url);
-     // R

**Actionable Steps:**
1. Modified 1 files
2. identifier: Handle
3. identifier: POST
4. identifier: PUT
5. identifier: DELETE

## 📐 Conventions & Best Practices

### Project Conventions
- 📐 **Fixed null crash in LanguageContext — avoids unnecessary re-renders in React — confirmed 3x** — -   Receipt,
+ } from 'lucide-react';
-   Eye,
+ import { useState, useMemo, useCallback, useEffect,
- 📐 **Fixed null crash in Failed — prevents null/undefined runtime crashes — confirmed 3x** — -     }
+       return json({ success: false, error: 'Failed to apply template' }, { status: 500 });
- 📐 **Strengthened types index** — -                     style={{ backgroundColor: theme.primary, color: '#fff' }}
+                   
- 📐 **Fixed null crash in Also — reduces initial bundle size with code splitting — confirmed 3x** — -     
+ 
-     return json({ success: true, message: 'themeSaved' });
+     // Also save to unified
- 📐 **convention in store-live-editor.tsx** — File updated (external): apps/web/app/routes/store-live-editor.tsx

Content summary (3384 lines):
/*

## 🤔 Decisions & Trade-offs

- **decision in app.orders._index.tsx** — -             .then((val) => ({ phone, val }))
+             .then((val: any) => ({ phone, val }))
-
- **Optimized FRAUD_CHECK** — -         { success: true, intent: 'FRAUD_CHECK', orderId, riskResult: { ...normalizedResultObj, fro
- **decision in app.orders._index.tsx** — -             raw.successRate = 100 - (raw.riskScore ?? raw.score ?? Number(raw.isHighRisk ?? 0) * 1
- **decision in app.store-design.tsx** — -             {templates.map((template) => {
+             {templates.map((template: any) => {
-    
- **decision in app.store-design.tsx** — -     storeMode: store[0].mode || 'store',
+     storeMode: (store[0] as any).mode || 'store',

📌 I

---
*Auto-generated by BrainSync 🧠 | 39 patterns | 2026-03-27*
