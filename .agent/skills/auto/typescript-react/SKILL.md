---
name: typescript-react
description: "Typescript React for multi-store-saas. 1 conventions, 2 fixes."
domain: typescript-react
composesFrom:
  - react
triggers:
  - glob: "**/*.tsx"
autoGenerated: true
enabled: true
---

# Typescript React

Auto-compiled from **17 real patterns** in **multi-store-saas**. This skill is auto-routed to agents when working on typescript-react files.

## 🔧 Problem Playbooks

### Fixed null crash in Brain — parallelizes async operations for speed
- import { customers, stores, orders, products, orderItems, pageViews, carts, checkoutSessions } from '@db/schema';
+ import {
- import { requireSuperAdmin } from '~/services/auth.server';
+   customers,
- import { callAIWithSystemPrompt } from '~/services/ai.server';
+   stores,
- import { useState, useEffect } from 'react';
+   orders,
- import {
+   products,
-   Brain,
+   orderItems,
-   Tren

**Actionable Steps:**
1. Modified 1 files
2. identifier: Brain
3. identifier: TrendingUp
4. identifier: TrendingDown
5. identifier: AlertTriangle

### Fixed null crash in Handle — prevents null/undefined runtime crashes
- export async function loader({ request, context }: LoaderFunctionArgs) {
+ /**
-   // Resolve store
+  * Handle POST/PUT/DELETE to unknown routes (bots probing /graphql, /wp-admin, etc.)
-   const storeContext = await resolveStore(context, request);
+  */
- 
+ export async function action({ request }: ActionFunctionArgs) {
-   if (!storeContext) {
+   const url = new URL(request.url);
-     // R

**Actionable Steps:**
1. Modified 1 files
2. identifier: Handle
3. identifier: POST
4. identifier: PUT
5. identifier: DELETE

## 📐 Conventions & Best Practices

### Project Conventions
- 📐 **convention in store-live-editor.tsx** — File updated (external): apps/web/app/routes/store-live-editor.tsx

Content summary (3384 lines):
/*

## 🤔 Decisions & Trade-offs

- **decision in SettingsPanel.tsx** — File updated (external): apps/web/app/components/builder/SettingsPanel.tsx

Content summary (514 lin
- **decision in SectionList.tsx** — File updated (external): apps/web/app/components/builder/SectionList.tsx

Content summary (239 lines
- **trade-off in AccountHeader.tsx** — File updated (external): apps/web/app/components/account/AccountHeader.tsx

Content summary (186 lin

---
*Auto-generated by BrainSync 🧠 | 17 patterns | 2026-03-24*
